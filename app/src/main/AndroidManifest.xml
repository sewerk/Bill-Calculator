<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.srw.billcalculator">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BillCalculator"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="a7369bc28192a2f175b7c8d7473f0e61a756366c" />

        <activity android:name=".form.MainActivity" android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".bill.PgeBillActivity" android:label="@string/energy_bill" />

        <activity android:name=".bill.PgnigBillActivity" android:label="@string/gas_bill" />

        <activity android:name=".bill.TauronBillActivity" android:label="@string/energy_bill" />

        <activity android:name=".settings.activity.SettingsActivity" android:label="@string/settings_label" />

        <activity android:name=".settings.activity.ProviderSettingsActivity" />

        <activity android:name=".settings.activity.ProviderSettingsHelpActivity" android:label="@string/action_help"
                  android:theme="@style/DialogTheme" />

        <activity android:name=".AboutActivity" android:label="@string/about_label"
                  android:theme="@style/DialogTheme" />

        <activity android:name=".history.HistoryActivity" android:label="@string/history_label" />

        <service android:name=".bill.service.PgnigBillStoringService" />

        <service android:name=".bill.service.PgeBillStoringService" />

        <service android:name=".bill.service.TauronBillStoringService" />

    </application>

</manifest>
