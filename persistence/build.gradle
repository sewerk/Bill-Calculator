apply plugin: 'java'

dependencies {
    compile('de.greenrobot:greendao:1.3.1')
    compile('com.google.android:android:4.0.+')
}

sourceSets {
    main {
        java.srcDirs = ['src/main/java', 'src/main/java-gen']
    }
}